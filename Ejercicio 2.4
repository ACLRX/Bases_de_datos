SELECT * FROM SucursalPLSQL
WHERE CIUDAD = 'Madrid' 

Esta consulta retorna cuantas sucursales estan ubicadas en la ciudad que pida el usuario. Para la base se registran 0 registros, es decir
que no hay ninguna sucursal en Madrid. 
