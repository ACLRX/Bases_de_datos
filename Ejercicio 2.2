SELECT * FROM AutoPLSQL
WHERE MARCA = 'Toyota'

Retorna la cantidad de Autos que son de marca Toyota, para la base de datos no se regitra ningun registro, 
esto significa que no hay ningún auto de la marca Toyota. 
